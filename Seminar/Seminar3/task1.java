package Seminar.Seminar3;

import java.util.ArrayList;
import java.util.LinkedList;
import java.util.List;

// Коллекции
public class task1 {
    // public static void main(String[] args) {

    /**
     * Java Document (вызывается /**) ставиться над методом
     * 
     */

    // List list = new LinkedList();// передаст только строку //L.add(new
    // Animal("F", "d");
    // List<Integer> R = new LinkedList(); // передаст число // нельзя у строки
    // запросить число и наооброт
    // list.add(true);
    // list.add(2);

    // List<Integer> l = List.of(1,2,3,4,5);
    // List<Integer> r = new LinkedList<>(); // Создали изменяемый список
    // r.addAll(List.of(1,2,3,4)); // добавили иммутабельную коллекцию
    // l.add(4);
    // r.remove(3);// удалили из иммутабельной коллекции

    // for (Object o : list) { // классы можно превести только похожие....должно
    // быть общее наследование //
    // // динамическая типизация for ( o: list)
    // float f = (float) 1222212d;
    // if (o instanceof String) {
    // String str = (String) o;
    // // System.out.println(((String) o).length());
    // System.out.println(str.length());
    // }

    // Integer.parseInt("1"); // принимает только строку
    // }
    // System.out.println(list);
    // }
}